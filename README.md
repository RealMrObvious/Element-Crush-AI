# Group 158

Trains an AI using RL to play a match 3 game.

Base game designed by Harry Chen https://github.com/theharrychen/Element-Crush/tree/master

Agent/Code can be run by running `train.py` using `python train.py`

requirements should be installable using `pip install -r requirements.txt` (untested)
